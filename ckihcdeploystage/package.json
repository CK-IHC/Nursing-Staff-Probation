{
  "name": "nursing-staff-probation",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that hosts the Nursing Staff Probation Management frontend as static assets. Backend/API lives in Google Apps Script.",
  "scripts": {
    "build": "echo 'static frontend - no build step required'",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.114.0"
  }
}
